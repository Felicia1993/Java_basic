package com.thread.demo.juc;
public class Volatile{
    /**
     *volatile关键字为实例域的同步访问提供了一种免锁机制。如果生命一个域为volatile，那么编译器和虚拟机就知道该域是可能被另一个线程并发更新的
     *
     * final变量，可以安全地访问一个共享域
     * final Map<String ,Double> accounts = new HashMap<>()
     */
}